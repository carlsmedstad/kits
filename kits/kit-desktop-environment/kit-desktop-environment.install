# shellcheck shell=bash

post_install() {
  echo "greetd is enabled by preset on first boot. On an existing system, apply"
  echo "the preset and reboot:"
  echo ""
  echo "  sudo systemctl preset greetd"
}
